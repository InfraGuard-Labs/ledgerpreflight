# Optional private validation

Place only artifacts you are licensed to use here for manual validation. This directory is ignored by Git and excluded from Docker contexts. Development and release validation never reads it. Do not distribute its contents. No actual node data is required for building or testing LedgerPreflight.
